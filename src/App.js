// TODO: Three pages with smooth linking fade - one for info for users telling them this page is not ready so we're
// redirecting you to counter page - the counter page - and the last page will be the celebration and introdutions
import AppRoutes from "./routes";
const App = () => {
    return (
        <div>
            <AppRoutes />
        </div>
    )
}

export default App;
